version: 2.1
jobs:
  build:
    docker:
      - image: 'hub.docker.prod.walmart.com/circleci/android:api-29-node'
    steps:
      - checkout
      - run:
          name: Check node version
          command: node -v