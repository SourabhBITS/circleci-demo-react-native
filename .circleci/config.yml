version: 2.1
jobs:
  build:
    docker:
      - image: 'hub.docker.prod.walmart.com/circleci/android:api-30-alpha'
    steps:
      - checkout
      # - run: node --version
      - run:
          name: Download Dependencies
          command: ./gradlew androidDependencies